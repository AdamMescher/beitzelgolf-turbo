import * as React from 'react';
import styled from 'styled-components';

type Service = {
  service: string;
  cost: string;
  turnaround: string;
};

const Table = () => {
  <div>table</div>;
};

export default Table;
